{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "echo 'This is now an API-only backend. Frontend has been moved to a separate project.'",
        "build": "echo 'No frontend build needed for API-only backend.'"
    },
    "devDependencies": {},
    "dependencies": {}
}
